"""I/O helpers — structure databases, basis-file emission, etc.

Public modules:

* ``vibe_basis.io.structures`` — the 39-compound pob-paper test-set
  crystal database. ``Structure``/``StructureAtom`` dataclasses,
  helper expansion functions, the ``STRUCTURES`` dict + the
  ``all_structures()`` / ``in_table()`` convenience accessors.
"""
